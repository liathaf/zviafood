import { CommentRelativeTimePipe } from './comment-relative-time.pipe';

describe('CommentRelativeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentRelativeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
