import { AddCountStepPrepPipe } from './add-count-step-prep.pipe';

describe('AddCountStepPrepPipe', () => {
  it('create an instance', () => {
    const pipe = new AddCountStepPrepPipe();
    expect(pipe).toBeTruthy();
  });
});
